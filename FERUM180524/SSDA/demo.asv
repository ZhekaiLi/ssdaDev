%% demo
EX2;    % call model
global net gpopt ep;


%% Initial function

%ferum

%% ZK: new ferum function that compare the 
% For "case 24", 
% the initial ferum.m function use ss_da()
% the modificated ferum_compareGPMs use ss_da_compareGPMs()
% ss_da_compareGPMs()  keep the calculated GP model in each step
% In the prediction, the function will do calculation by each GP model
% and choose the results with smallest variation

ferum_compareGPMs

